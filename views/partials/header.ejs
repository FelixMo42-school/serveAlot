<nav class="navbar navbar-expand-lg navbar-dark bg-primary static-top" style="margin-bottom: 16px">
    <div class="container">
        <span class="navbar-brand" href="#">> <%= title.toUpperCase() %></span>

        <ul class="navbar-nav">
            <% if (session) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/profile">profile</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout">logout</a>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/login">login</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>