<nav class="navbar navbar-expand-lg navbar-light bg-light static-top" style="margin-bottom: 16px">
    <div class="container">
        <a class="navbar-brand" href="#">> LOG IN</a>

        <ul class="nav">
            <% if (user) {%>
                <li class="nav-item" id="logout">
                    <a href="/logout">logout</a>
                </li>
            <% } else { %>
                <li class="nav-item" id="login">
                    <a href="/login">login</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>